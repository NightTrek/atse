id: indexing
name: "Indexing and Search (Full-text, n-gram, Tokenization)"
question: |
  Describe how Dgraph implements indexing for string predicates, including term,
  full-text, trigram / n-gram, and other relevant index types. Explain how
  tokenization works, how index data is stored, and how indexes are used during
  query planning and execution. Identify the key packages and source files
  involved in these behaviors.

entry_repo: "https://github.com/dgraph-io/dgraph.git"
entry_ref: "v25.0.0"

expected_output_schema: "v1"
run_config:
  runs_per_agent: 10
  max_duration_minutes: 20
